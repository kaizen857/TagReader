Task 7 evidence

Changed files:
- src/formats/cue/CueReader.cpp
- src/formats/cue/CueTiming.cpp
- src/formats/cue/CueTiming.hpp
- test/CMakeLists.txt
- test/regression/cue_timing_catch2_tests.cpp
- .omo/notepads/read-cue-sheet/learnings.md
- CMakeLists.txt

Verification:
- `cmake --build --preset default` ✅
- `ctest --test-dir build/default -R 'cue timing converts frame indexes to microseconds|cue timing derives duration from next track and audio tail|cue timing handles multiple files independently|cue timing rejects invalid backward and overflow indexes|cue mapping applies track and album fields|cue mapping preserves audio fields when cue has no override|cue read path stays unchanged for ordinary audio' --output-on-failure` ✅
- Pure LOC: `CueReader.cpp` 173, `CueTiming.cpp` 96, `cue_timing_catch2_tests.cpp` 94

Behavior covered:
- 75fps timing conversion
- track offsets from `INDEX 01`
- per-file duration derivation
- last-track duration from audio tail
- multi-file timing independence
- invalid/backward/overflow timing rejection
- task 6 mapping and ordinary `Read()` stability

Task 9 Ogg Vorbis parser migration evidence
Date: 2026-05-31

Scope note:
- `git diff` against HEAD includes inherited Tasks 1-8 refactor background because HEAD is pre-refactor; do not use HEAD as a restore source or as the only Task 9 boundary.
- Task 9 changed the active build graph and current working tree so Ogg page/packet scanning lives in `src/formats/ogg-vorbis/OggVorbisParser.*` and `src/TagReader.cpp` dispatches to `tagreader_ogg_vorbis`.

Files created/modified for Task 9:
- Created: `src/formats/ogg-vorbis/OggVorbisParser.hpp`
- Created: `src/formats/ogg-vorbis/OggVorbisParser.cpp`
- Modified: `src/TagReader.cpp`
- Modified: `CMakeLists.txt`
- Added evidence: `.omo/evidence/task-9-ogg-diff.txt`
- Added evidence: `.omo/evidence/task-9-ogg-malformed.txt`
- Appended notepad entries: `.omo/notepads/tagreader-refactor-implementation/learnings.md`, `.omo/notepads/tagreader-refactor-implementation/issues.md`

Build graph update:
- `CMakeLists.txt` now compiles `src/formats/ogg-vorbis/OggVorbisParser.cpp` in `TagReaderCore`.
- Empty `src/formats/ogg-vorbis/OggVorbisMetadata.cpp` remains in the build as a scaffold stub; the real parser source is explicit and adjacent.

Scanner ownership check:
Command:
`rg -n "ReadOggVorbisComments|ReadOggVorbisCommentEntries|ReadVorbisLyrics|VorbisStreamStage|kMaxOggPacketBytes|kMaxOggScannedBytes|kMaxOggPages" src`
Status: 0
Relevant result:
- Ogg scanner symbols (`ReadOggVorbisCommentEntries`, `VorbisStreamStage`, `kMaxOggPacketBytes`, `kMaxOggScannedBytes`, `kMaxOggPages`) appear under `src/formats/ogg-vorbis/OggVorbisParser.cpp`.
- `src/TagReader.cpp` has no scanner symbol matches.
- Other matches are unrelated FLAC/Vorbis entry-level calls (`ReadVorbisLyricsEntry`) in `src/formats/flac/FlacParser.cpp` and `src/formats/vorbis/VorbisCommentParser.cpp`.

Dispatch check:
Command:
`rg -n "tagreader_ogg_vorbis::ReadOggVorbisMetadata|tagreader_ogg_vorbis::ReadOggVorbisLyrics" src/TagReader.cpp`
Status: 0
Output:
`src/TagReader.cpp:1287:        tagreader_ogg_vorbis::ReadOggVorbisMetadata(context, metadata);`
`src/TagReader.cpp:1523:            tagreader_ogg_vorbis::ReadOggVorbisLyrics(context, lyrics);`

Vorbis entry semantics reuse:
- `src/formats/ogg-vorbis/OggVorbisParser.cpp` calls `tagreader_vorbis::ReadVorbisCommentEntry(metadata, entry)` for metadata entries.
- `src/formats/ogg-vorbis/OggVorbisParser.cpp` splits `KEY=VALUE` only for lyrics and calls `tagreader_vorbis::ReadVorbisLyricsEntry(lyrics, key, value)`.
- Generic Vorbis key/value semantics were not duplicated outside the scanner's packet-level comment-list walker.

Byte helper reuse:
- Ogg parser uses `tagreader_io::ReadRange`, `tagreader_io::ReadLE32`, and `tagreader_io::TryAddUintmax`.

Build verification:
Command: `cmake -S . -B build`
Status: 0
Output summary:
`-- Configuring done`
`-- Generating done`
`-- Build files have been written to: /home/kaizen857/cppProject(app_and_lib)/TagReader/build`

Command: `cmake --build build`
Status: 0
Output summary:
`[1/5] Building CXX object CMakeFiles/TagReaderCore.dir/src/formats/ogg-vorbis/OggVorbisParser.cpp.o`
`[2/5] Building CXX object CMakeFiles/TagReaderCore.dir/src/TagReader.cpp.o`
`[3/5] Linking CXX static library libTagReaderCore.a`
`[4/5] Linking CXX executable TagReaderSecuritySmoke`
`[5/5] Linking CXX executable TagReaderTest`

Comparable baseline stdout verification:
Command: Python runner over every `returncode == 0` entry with `stdout_file` in `.omo/evidence/baseline/tagreadertest-runs.json`.
Status: 0
Output:
`baseline comparable stdout entries: 8`
`mismatches: 0`

LSP diagnostics:
Command: `lsp_diagnostics` on `src/formats/ogg-vorbis/OggVorbisParser.cpp`
Status: pass
Output: `No diagnostics found`

Command: `lsp_diagnostics` on `src/formats/ogg-vorbis/OggVorbisParser.hpp`
Status: pass
Output: `No diagnostics found`

Command: `lsp_diagnostics` on `src/TagReader.cpp`
Status: pass
Output: `No diagnostics found`

Forbidden abstraction scan:
Command: `rg -n "dynamic_cast|typeid\(|visitor|Plugin|Registry|ITagParser|virtual .*Parser|NotImplemented|TODO|FIXME|HACK" src include`
Status: 1 (no matches)
Output: empty

`reinterpret_cast` inventory:
Command: `rg -n "reinterpret_cast" src/formats/ogg-vorbis src/TagReader.cpp`
Status: 0
Output:
`src/TagReader.cpp:617:    input.read(reinterpret_cast<char *>(buffer.data()), static_cast<std::streamsize>(buffer.size()));`
`src/TagReader.cpp:634:    const auto *ptr = reinterpret_cast<const unsigned char *>(text.data());`
`src/TagReader.cpp:721:    std::string atomType(reinterpret_cast<const char *>(header.data() + 4), 4);`
`src/TagReader.cpp:820:        const std::string_view atomType(reinterpret_cast<const char *>(header.data() + 4), 4);`
`src/TagReader.cpp:1218:    if (header.size() >= 4 && std::string_view(reinterpret_cast<const char *>(header.data()), 4) == "fLaC")`
`src/TagReader.cpp:1222:    if (header.size() >= 4 && std::string_view(reinterpret_cast<const char *>(header.data()), 4) == "OggS")`
`src/TagReader.cpp:1226:    if (header.size() >= 8 && std::string_view(reinterpret_cast<const char *>(header.data() + 4), 4) == "ftyp")`
`src/TagReader.cpp:1348:    const std::string_view raw(reinterpret_cast<const char *>(payload), payloadSize);`
`src/TagReader.cpp:1613:                const DecodedField field = DecodeRawText(std::string_view(reinterpret_cast<const char *>(payload.data() + 4), payload.size() - 4));`
`src/formats/ogg-vorbis/OggVorbisParser.cpp:81:        handler(std::string_view(reinterpret_cast<const char *>(data + cursor), commentLength));`
`src/formats/ogg-vorbis/OggVorbisParser.cpp:102:    return bytes.size() >= 7 && bytes[0] == packetType && std::string_view(reinterpret_cast<const char *>(bytes.data() + 1), 6) == "vorbis";`
`src/formats/ogg-vorbis/OggVorbisParser.cpp:130:        if (pageHeader.size() != 27 || std::string_view(reinterpret_cast<const char *>(pageHeader.data()), 4) != "OggS" || pageHeader[4] != 0)`
Interpretation:
- `src/TagReader.cpp` casts are unchanged pre-existing byte/string boundaries for input reads, UTF-8 validation, container detection, MP4 atom types, and MP4 text payloads.
- `src/formats/ogg-vorbis/OggVorbisParser.cpp` casts are the migrated original Ogg byte-to-string_view boundaries for Vorbis comment entries, Vorbis packet signature, and Ogg capture pattern; no new parser-level cast category was introduced.

Diff note:
Command: `GIT_MASTER=1 git diff --stat -- CMakeLists.txt src/TagReader.cpp src/formats/ogg-vorbis/OggVorbisParser.hpp src/formats/ogg-vorbis/OggVorbisParser.cpp`
Status: 0
Output was relative to pre-refactor HEAD and therefore included historical Tasks 1-8 movement:
`CMakeLists.txt    |   18 +`
`src/TagReader.cpp | 5537 ++++++++++-------------------------------------------`
`2 files changed, 1020 insertions(+), 4535 deletions(-)`
Because new `src/formats/ogg-vorbis/OggVorbisParser.*` files are under an untracked `src/formats/` tree relative to HEAD, the stat output does not isolate Task 9. The authoritative Task 9 checks are the current-source symbol scans, CMake build, LSP diagnostics, and baseline comparisons above.

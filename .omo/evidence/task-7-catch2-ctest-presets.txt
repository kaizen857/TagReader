1. Scope: wired `test/security/generate_samples.py` and `TagReaderSecuritySmoke` into CTest with `security` labels; kept old CLI runner intact.
2. Security workflow: `TagReaderSecurityGenerateSamples` prepares `build/default/test/security/samples`; `TagReaderSecuritySmoke` consumes the generated manifest and uses per-sample cover export subdirectories with directory reset before each sample.
3. Fuzz workflow: `TagReaderFuzzBoundedSmoke` is registered only when `TagReaderFuzz` exists; the default build does not register an unbounded fuzz run.
4. Verification: `cmake -S . -B build/default` succeeded; `cmake --build build/default --target TagReaderSecuritySmoke` succeeded; `ctest --test-dir build/default -L security --output-on-failure` passed; `ctest --test-dir build/default -N` listed only the two security tests and no fuzz run; `cmake --build --preset fuzz` succeeded with a clear warning that fuzzing is unavailable in this environment; `ctest --test-dir build/fuzz -N` listed no fuzz runtime tests because `TagReaderFuzz` was not built.
5. Manual smoke: `./build/default/TagReaderSecuritySmoke build/default/test/security/covers build/default/test/security/samples/id3v24_apic_png.mp3 build/default/test/security/samples/ogg_vorbis_picture_cover.ogg` passed after cover-dir isolation.

2026-06-08 Task 1: `test/regression/regression_tests.cpp` currently keeps `kTestCases` truthful at `TR-AUDIT-001` through `TR-AUDIT-025`; `TR-AUDIT-026` through `TR-AUDIT-031` have no existing references and should remain unlisted until implemented by their respective tasks.
2026-06-08 Task 1: Shared helpers needed by later work already exist in the regression harness (`RegressionTempRoot`, `RegressionEvidenceRoot`, APE item/footer/header/tag helpers, ID3v2.2 frame/tag helpers, endian append helpers, and cover export patterns), so no non-breaking helper edit was needed.
2026-06-08 Task 2: APEv2 footer tagSize is now treated as item bytes plus the 32-byte footer; optional header bytes live immediately before itemOffset and are validated but not included in item payload.
2026-06-08 Task 2: Regression helper AppendApeTag now emits spec-compliant APEv2 tagSize for both footer-only and header-present tags, so existing APE cases exercise the corrected semantics.
2026-06-08 Task 3: ID3v2.2 metadata must mirror lyrics parsing by rejecting unsupported header flags before frame parsing; legal flags=0x00 still preserve TT2 metadata.
2026-06-08 Task 5: `OpenContext()` now checks `std::filesystem::is_symlink(filePath, ec)` before file size, mtime, C++ stream open, and FFmpeg path open; TR-AUDIT-029 covers a generated regular MP3 control plus a symlink to that MP3.
2026-06-08 Task 6: No-iconv policy now has two explicit CMake switches: `TAGREADER_FORCE_DISABLE_ICONV_FOR_TESTS` to simulate missing Iconv and `TAGREADER_ALLOW_LATIN1_FALLBACK_WITHOUT_ICONV` to opt into the legacy Latin-1 fallback path.
2026-06-08 Task 6: `TR-AUDIT-030` is a compile-policy regression case; it prints `iconv-enabled` when `TAGREADER_HAS_ICONV` is compiled and `no-iconv-explicit-fallback` only when the no-iconv fallback macro is explicitly defined.
2026-06-08 Task 7: `Read(path)` now resolves empty cover export dir to `std::filesystem::temp_directory_path() / "tagreader-covers"`; explicit dirs remain caller-trusted and may be symlink dirs when the probe succeeds.
2026-06-08 Task 4: `ConvertImageToPng()` now uses `av_new_packet()` and `std::memcpy()` so FFmpeg owns padded decoder packet input while the existing `size > int::max()` guard remains in place.
2026-06-08 Task 8: Full integration verification passed in normal and sanitizer builds; TR-AUDIT-001 through TR-AUDIT-031 passed normally, TR-AUDIT-026 through TR-AUDIT-031 passed under sanitizer, and security sample generation/smoke completed with generated audio samples.
2026-06-08 Task 8: Commit audit confirmed separate recent commits for APE, ID3, cover packet, media symlink, no-iconv, cover policy, plus accepted follow-up `fix(cover): keep export policy regression portable`; evidence captured in `.omo/evidence/task-8-full-integration-error.txt`.

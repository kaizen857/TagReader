2026-06-08 Task 1: Initial invalid-case evidence capture used zsh variable name `status`, which is read-only; reran with `case_exit` and replaced `.omo/evidence/task-1-regression-harness-error.txt` with clean output showing `TR-AUDIT-999 unknown regression case` and exit 2.
2026-06-08 Task 2: Failure-first could not observe the intended title-empty behavior before implementation because TR-AUDIT-026 was intentionally unlisted/unimplemented; captured the pre-fix unknown-case exit in .omo/evidence/task-2-ape-tag-size-error.txt before adding the case.
2026-06-08 Task 3: Failure-first baseline for TR-AUDIT-027 also started as `unknown regression case` because the test was not yet registered; capture this in `.omo/evidence/task-3-id3-v22-flags-error.txt` before the parser/test fix.
2026-06-08 Task 5: Failure-first baseline for TR-AUDIT-029 was `unknown regression case` because the case was intentionally unlisted before implementation; captured in `.omo/evidence/task-5-input-symlink-order-error.txt`.
2026-06-08 Task 6: Failure-first baseline showed `TAGREADER_FORCE_DISABLE_ICONV_FOR_TESTS=ON` was previously ignored because the option did not exist; CMake warned the variable was unused and still found Iconv.
2026-06-08 Task 6: First allowed no-iconv build exposed the existing multi-line `#warning` as invalid C++ preprocessing on GNU; replaced it with a single `#warning` line and reran the allow build/test successfully.
2026-06-08 Task 7: Failure-first baseline for TR-AUDIT-031 was `unknown regression case` because the case was intentionally unlisted before implementation; captured in `.omo/evidence/task-7-cover-export-policy-error.txt`.
2026-06-08 Task 7 follow-up: `TR-AUDIT-031` initially used POSIX-only `<sys/stat.h>`, `<unistd.h>`, `chmod`, `mode_t`, and `geteuid` unconditionally; guarded them so non-POSIX builds platform-skip the non-writable-dir subcase instead of failing compilation.
2026-06-08 Task 4: Failure-first baseline for TR-AUDIT-028 was `unknown regression case` because the case was intentionally unlisted before implementation; captured in `.omo/evidence/task-4-cover-packet-padding-error.txt`.
2026-06-08 Task 8: Initial integration evidence used `seq -w` for 1..31, producing two-digit IDs (`TR-AUDIT-01`) that the harness reported as unknown; reran corrected strict three-digit `TR-AUDIT-001`..`TR-AUDIT-031` and sanitizer `TR-AUDIT-026`..`TR-AUDIT-031` successfully in `.omo/evidence/task-8-full-integration.txt`.
2026-06-08 F1 correction: Failure-first unknown-case baselines for Tasks 2/3/4/5/7 are a known limitation of Task 1 requiring truthful `kTestCases`/`--list` output and no broken placeholders before implementation. They are not a remaining product/test blocker because each case was added with its fix, covered by task-specific post-fix tests, dedicated commits, normal Task 8 `TR-AUDIT-001`..`TR-AUDIT-031`, and sanitizer Task 8 `TR-AUDIT-026`..`TR-AUDIT-031`.

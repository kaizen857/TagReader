- 前半段迁移的主要风险不是 Catch2 本身，而是旧回归 case 里混入了环境相关假设；例如默认封面目录要与生产逻辑一致，否则旧 runner 和新测试会出现假失败。
- 下一段 `TR-AUDIT-032..056` 复用同一 monolith 时，仍然需要避免把默认目录、缓存目录或 symlink 前置清理逻辑写死到旧假设里。
- `TR-AUDIT-056` 的 forbidden API 检查必须继续留在已有回归逻辑里；Catch2 只包装，不重写断言表达式。
- security smoke 的 cover cache 污染测试必须在独立 cover 子目录下跑，否则同一构建树里前一次运行留下的污染文件会串到后续样本。
- 删除旧 runner 后，`ctest -N` 的编号稳定性要继续依赖 Catch2 目标的 discover 顺序；后续若再拆分专项 wrapper，需要保持 `TR-AUDIT-001..056` 连续可追踪。
- 文档曾经残留“没有单元测试框架”“不要用 `ctest`”这类旧说法，迁移后必须统一改成 Catch2 + CTest + Presets 流程，避免新老说明互相打架。
